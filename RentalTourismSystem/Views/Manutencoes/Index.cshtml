@model IEnumerable<RentalTourismSystem.Models.ManutencaoVeiculo>

@{
    ViewData["Title"] = "Manutenções de Veículos";
}

<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-md-12">
            <h2><i class="fas fa-tools"></i> Manutenções de Veículos</h2>
            <p class="text-muted mb-0">Acompanhe serviços, prazos, custos e disponibilidade da frota.</p>
        </div>
    </div>

    <!-- Estatísticas -->
    <div class="row g-3 mb-4">
        <div class="col-md-3">
            <div class="card dashboard-card h-100">
                <div class="card-body">
                    <i class="fas fa-list fa-2x mb-2"></i>
                    <h3 class="stat-number">@ViewBag.TotalManutencoes</h3>
                    <p class="stat-label mb-0">Total de Manutenções</p>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card dashboard-card h-100">
                <div class="card-body">
                    <i class="fas fa-calendar-check fa-2x mb-2"></i>
                    <h3 class="stat-number">@ViewBag.ManutencoesAgendadas</h3>
                    <p class="stat-label mb-0">Agendadas</p>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card dashboard-card h-100">
                <div class="card-body">
                    <i class="fas fa-cog fa-2x mb-2"></i>
                    <h3 class="stat-number">@ViewBag.ManutencoesEmAndamento</h3>
                    <p class="stat-label mb-0">Em Andamento</p>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card dashboard-card h-100">
                <div class="card-body">
                    <i class="fas fa-money-bill-wave fa-2x mb-2"></i>
                    <h3 class="stat-number">@ViewBag.CustoTotal.ToString("C")</h3>
                    <p class="stat-label mb-0">Custo Total</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Filtros -->
    <div class="card mb-4">
        <div class="card-header">
            <h5 class="mb-0"><i class="fas fa-filter"></i> Filtros de Pesquisa</h5>
        </div>
        <div class="card-body">
            <form asp-action="Index" method="get">
                <div class="row g-3 align-items-end">
                    <div class="col-md-3">
                        <div class="form-group">
                            <label for="filtroVeiculo" class="form-label">Veículo</label>
                            <select id="filtroVeiculo" name="veiculoId" class="form-select" asp-items="ViewBag.VeiculoIdFiltro">
                                <option value="">Todos os veículos</option>
                            </select>
                        </div>
                    </div>
                    <div class="col-md-2">
                        <div class="form-group">
                            <label for="filtroStatus" class="form-label">Status</label>
                            <select id="filtroStatus" name="statusId" class="form-select" asp-items="ViewBag.StatusIdFiltro">
                                <option value="">Todos os status</option>
                            </select>
                        </div>
                    </div>
                    <div class="col-md-2">
                        <div class="form-group">
                            <label for="filtroTipo" class="form-label">Tipo</label>
                            <select id="filtroTipo" name="tipoId" class="form-select" asp-items="ViewBag.TipoIdFiltro">
                                <option value="">Todos os tipos</option>
                            </select>
                        </div>
                    </div>
                    <div class="col-md-2">
                        <div class="form-group">
                            <label for="filtroInicio" class="form-label">Data Início</label>
                            <input id="filtroInicio" type="date" name="dataInicio" class="form-control" value="@ViewBag.DataInicio" />
                        </div>
                    </div>
                    <div class="col-md-2">
                        <div class="form-group">
                            <label for="filtroFim" class="form-label">Data Fim</label>
                            <input id="filtroFim" type="date" name="dataFim" class="form-control" value="@ViewBag.DataFim" />
                        </div>
                    </div>
                    <div class="col-md-1">
                        <div class="form-group">
                            <button type="submit" class="btn btn-primary w-100" aria-label="Aplicar filtros"><i class="fas fa-search"></i></button>
                        </div>
                    </div>
                </div>
                <div class="row g-3 align-items-center mt-1">
                    <div class="col-md-3">
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" name="preventiva" value="true" @(ViewBag.Preventiva == true ? "checked" : "") />
                            <label class="form-check-label">Apenas Preventivas</label>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" name="urgente" value="true" @(ViewBag.Urgente == true ? "checked" : "") />
                            <label class="form-check-label">Apenas Urgentes</label>
                        </div>
                    </div>
                    <div class="col-md-6 text-end">
                        <a asp-action="Index" class="btn btn-secondary"><i class="fas fa-eraser"></i> Limpar Filtros</a>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <!-- Botões de Ação -->
    @if (User.IsInRole("Admin") || User.IsInRole("Manager"))
    {
        <div class="mb-3">
            <a asp-action="Create" class="btn btn-success"><i class="fas fa-plus"></i> Nova Manutenção</a>
            <a asp-action="Relatorio" class="btn btn-info"><i class="fas fa-chart-bar"></i> Relatório</a>
        </div>
    }

    <!-- Tabela de Manutenções -->
    <div class="card">
        <div class="card-body">
            @if (Model.Any())
            {
                <div class="table-responsive">
                    <table class="table table-striped table-hover">
                        <thead class="thead-dark">
                            <tr>
                                <th>Veículo</th>
                                <th>Tipo</th>
                                <th>Status</th>
                                <th>Data Agendada</th>
                                <th>Quilometragem</th>
                                <th>Custo Total</th>
                                <th>Marcadores</th>
                                <th>Ações</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        <strong>@item.Veiculo?.Marca @item.Veiculo?.Modelo</strong><br />
                                        <small class="text-muted">@item.Veiculo?.Placa</small>
                                    </td>
                                    <td>@item.TipoManutencao?.Nome</td>
                                    <td>
                                        @if (item.StatusManutencao?.Status == "Agendada")
                                        {
                                            <span class="badge bg-warning text-dark">@item.StatusManutencao?.Status</span>
                                        }
                                        else if (item.StatusManutencao?.Status == "Em Andamento")
                                        {
                                            <span class="badge bg-info text-dark">@item.StatusManutencao?.Status</span>
                                        }
                                        else if (item.StatusManutencao?.Status == "Concluída")
                                        {
                                            <span class="badge bg-success">@item.StatusManutencao?.Status</span>
                                        }
                                        else if (item.StatusManutencao?.Status == "Cancelada")
                                        {
                                            <span class="badge bg-danger">@item.StatusManutencao?.Status</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">@item.StatusManutencao?.Status</span>
                                        }
                                    </td>
                                    <td>
                                        @item.DataAgendada.ToString("dd/MM/yyyy HH:mm")<br />
                                        @if (item.DiasAteAgendamento > 0)
                                        {
                                            <small class="text-muted">Em @item.DiasAteAgendamento dia(s)</small>
                                        }
                                        else if (item.DiasAteAgendamento == 0)
                                        {
                                            <small class="text-warning"><strong>Hoje</strong></small>
                                        }
                                        else
                                        {
                                            <small class="text-danger">Atrasada</small>
                                        }
                                    </td>
                                    <td>@item.QuilometragemAtual.ToString("N0") km</td>
                                    <td>
                                        <strong>@item.CustoTotal.ToString("C")</strong>
                                    </td>
                                    <td>
                                        @if (item.Preventiva)
                                        {
                                            <span class="badge bg-primary" title="Manutenção Preventiva"><i class="fas fa-shield-alt"></i> Preventiva</span>
                                        }
                                        @if (item.Urgente)
                                        {
                                            <span class="badge bg-danger" title="Urgente"><i class="fas fa-exclamation-triangle"></i> Urgente</span>
                                        }
                                        @if (item.EmGarantia)
                                        {
                                            <span class="badge bg-success" title="Em Garantia"><i class="fas fa-certificate"></i> Garantia</span>
                                        }
                                    </td>
                                    <td>
                                        <div class="btn-group" role="group">
                                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info" title="Detalhes">
                                                <i class="fas fa-eye"></i>
                                            </a>
                                            @if (User.IsInRole("Admin") || User.IsInRole("Manager"))
                                            {
                                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning" title="Editar">
                                                    <i class="fas fa-edit"></i>
                                                </a>
                                            }
                                            @if (User.IsInRole("Admin"))
                                            {
                                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger" title="Excluir">
                                                    <i class="fas fa-trash"></i>
                                                </a>
                                            }
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="alert alert-info">
                    <i class="fas fa-info-circle"></i> Nenhuma manutenção encontrada com os filtros aplicados.
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            // Inicializar tooltips
            $('[data-toggle="tooltip"]').tooltip();
        });
    </script>
}
